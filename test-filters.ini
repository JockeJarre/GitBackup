[GitBackup]
RootDir=C:\temp\TestSource
BackupDir=C:\temp\TestBackupFiltered
BackupMethod=index
Verbose=true

# File size filtering - only files between 1KB and 50KB
MinFileSizeBytes=1KB
MaxFileSizeBytes=50KB

# Exclude binary files (text files only)
ExcludeBinaryFiles=true

GitUserName=TestUser
GitUserEmail=test@example.com
